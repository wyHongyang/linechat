/**
 * ajax validator
 */

(function($){
	$.fn.extend({
		validator:function(options){
			var that = this;
			
			var setting = {
					
			};
			
			
			return this.each(function(){
				
			});
		}
	});
})(jQuery);